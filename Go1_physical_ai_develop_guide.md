# [Go1 Team] Ultra-Lightweight Integration & Development Guide

이 문서는 사족보행 로봇 **Unitree Go1**을 자원이 제한된 라즈베리파이 환경에서 효율적으로 통합하기 위한 기술 지침입니다. 우리 시스템은 **"최소한의 릴레이, 최대한의 원격 연산"** 원칙을 따릅니다.

## 1. 현 상황 및 하이브리드 전략 (Hybrid Strategy)

Go1은 EP01과 달리 패킷 구조가 복잡하여 단순 UDP 포워딩만으로는 통신 유지가 어렵습니다. 하지만 라즈베리파이의 램이 1GB뿐이므로, 무거운 SDK 전체를 올리는 대신 아래의 하이브리드 방식을 채택합니다.

- **Relay (Pi):** Go1 SDK의 **Python Binding(.so)** 또는 **C++ Standalone Binary**만 활용하여 통신 하트비트 유지 및 Raw 데이터 추출만 담당합니다. (ROS 설치 금지)
- **Worker (Laptop):** ROS1, Unitree SDK 전체 환경, 복잡한 보행 알고리즘은 모두 **노트북의 Go1용 Docker 컨테이너**에서 실행합니다.

**라즈베리파이 아키텍처 환경에 맞게 sdk 빌드해서 빼온 .so파일 backup 브랜치에 프로젝트 폴더와 같이 업로드 해뒀습니다. 필요 시 활용해주세요. (테스트 안 해봐서 오류는 보장 못합니다.)**
혹시 unitree sdk 관련 다른 python 바인딩을 위한 파일이 필요하다면 라즈베리파이에서 도커 컨테이너 만들어서 sdk 빌드하고 `lib/python/arm64` 내의 파일들 가져오면 됩니다. 편의를 위해 파일 이름 수정했으니 기존엔 `robot_interface.so` 이름이 아닐 겁니다.(확실치는 않습니다. 전에 연구 진행하며 느낀 점이니 그저 참고 바랍니다.) 빌드 및 파일 저장 작업이 끝난 후 라즈베리파이의 최적화를 위해 클린업 진행하는 것을 권장합니다. 

## 2. Go1팀의 핵심 개발 과제 (Technical Tasks)

### **A. 초경량 Go1 릴레이 구현 (Pi) (테스트 추천)**

라즈베리파이 환경(Python 3.9-slim)에 맞춰 SDK를 경량화해야 합니다.

1. **SDK 컴파일:** Unitree SDK를 C++로 빌드하여 파이썬에서 불러올 수 있는 `.so` 라이브러리 파일만 추출합니다.
2. **통신 루프:** Go1 로봇과 UDP 8082(Low-level) 또는 8090(High-level) 포트로 통신하며, 수신된 바이트 데이터를 해석하지 않고 그대로 Hub Server로 던집니다.
3. **환경 최적화:** `apt-get`으로 `liblcm-dev` 등 필수 런타임만 설치하여 컨테이너 크기를 최소화합니다 (16GB SD카드 용량 확보용).

### **B. `go1_worker` 컨테이너 구현 (Laptop)**

노트북 서버의 강력한 성능을 활용하여 실제 로봇의 '뇌'를 구축합니다.

1. **환경 구축:** `ubuntu:bionic` 이미지 기반으로 Python 2.7/3.x, ROS1 Melodic 등 Go1 SDK가 요구하는 최적의 환경을 자유롭게 구성합니다. `server_brain/go1_brain/Dockerfile.go1` 파일에서 수정 가능.
2. **데이터 복원:** Hub로부터 받은 Raw Byte 데이터를 Unitree SDK 구조체에 매핑하여 로봇의 현재 상태(관절, 기울기 등)를 복원합니다.
3. **명령 생성:** Unity팀이 보낸 이동 명령(`command_request`)을 Go1의 보행 제어 파라미터로 변환하여 다시 Hub -> Relay -> Robot 순으로 전달합니다.

---

## 3. 통신 및 네트워크 가이드

### **UDP 브로드캐스팅 환경**

- **네트워크 모드:** Go1의 특수 패킷 수신을 위해 라즈베리파이 컨테이너는 반드시 `-network host` 모드로 실행되어야 합니다.
- **포트 바인딩:** 로봇 내부 PC와의 통신을 위해 8000~8090번대 포트 점유를 확인하세요.
- **MTU 설정:** 고주파 데이터 송수신 시 패킷 잘림 현상이 발생할 수 있으므로, 네트워크 인터페이스의 MTU 값(기본 1500) 조정을 염두에 두어야 합니다.

---

## 4. Go1팀 개발 로드맵 (Roadmap)

1. **1단계 (SDK 경량화 연구):** 전체 SDK 중 통신에 필수적인 라이브러리만 추출하여 라즈베리파이(Python 3.9-slim)에서 `import` 가능한지 검증.
2. **2단계 (UDP 통신 구축):** 로봇-파이 간의 UDP 핸드셰이킹을 성공시키고, `relay_to_hub` 이벤트를 통해 Hub 서버 터미널에 Go1 데이터가 찍히는지 확인.
3. **3단계 (노트북 워커 연동):** 노트북 컨테이너에서 Go1 SDK를 통해 파이에서 넘어온 데이터를 정상적인 로봇 상태 객체로 변환.
4. **4단계 (제어 루프 테스트):** Unity 명령 -> 노트북 워커 -> 릴레이 -> 실제 로봇 보행으로 이어지는 루프 완성.

---

## 5. 주의 사항 및 팁

- **SD카드 관리:** 16GB는 매우 좁습니다. 빌드는 반드시 노트북에서 수행하고 파이에서는 `docker pull`만 진행하세요. 수시로 `docker system prune`을 실행해 캐시를 삭제해야 합니다.
- **의존성 분리:** "파이는 통신만, 노트북은 연산만" 원칙을 어기고 파이에서 ROS를 돌리려 하면 램 부족으로 시스템이 멈춥니다.

---

**💡 Go1팀에게 전달할 메시지:**

> "핵심 도전 과제는 **'가장 무거운 로봇을 가장 가벼운 인프라로 제어하는 것'**입니다. 환경의 제약을 Docker와 분산 연산으로 극복하는 과정 자체가 훌륭한 연구 결과가 될 것입니다. 우선 SDK 라이브러리(.so) 다운로드 및 테스트부터 시작해 봅시다."
> 

---
